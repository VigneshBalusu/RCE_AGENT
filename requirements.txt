# --- PyTorch (CPU Version) ---
# This reduces the deployment size by ~500MB (Critical for free tier)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
sentence-transformers>=3.0.0

# --- Core Web Framework ---
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0

# --- RAG & AI (For main.py) ---
# STRICT PINNING TO PREVENT DEPENDENCY HELL
langchain-classic==1.0.1
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-huggingface>=0.1.0
langchain-chroma>=0.1.4
chromadb>=0.5.0
rank_bm25

# --- Voice & Audio (For tts.py) ---
edge-tts

# --- Cloud Fixes (CRITICAL) ---
# Required for ChromaDB to run on Render/Linux
pysqlite3-binary